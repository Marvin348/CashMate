import type { Transaction } from "@/types/transaction";

type GridTransactionsProps = {
  data: Transaction[];
};

const GridTransactions = ({ data }: GridTransactionsProps) => {
  return <div>GridTransactions</div>;
};
export default GridTransactions;
